interface:
  display_name: "EIDP"
  short_description: "Repo-specific patterns and workflows for EIDP"
  default_prompt: "Use the EIDP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true