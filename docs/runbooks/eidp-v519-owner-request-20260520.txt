EIDP v519 Owner / Operator Request
Date: 2026-05-20

Purpose
-------
v519 is the current Mac-side package/source verified candidate. It is not v1.0
approval and it is not permission to switch the active weekly task.

Do not use this request to start manual deployment until the release decision is
explicit. Current status remains NOT COMPLETE.

Use this package
----------------
Package:
  C:\EIDP-staging\eidp-windows-v519.zip

Expected SHA256:
  fbc2ae0016b7b293c0fd534d7b3e7eb881f74205fa6df19acda42a8d21ba195a

Suggested side-by-side root:
  %USERPROFILE%\EIDP-v519-24fa09a-env0

Current active root to preserve:
  %USERPROFILE%\EIDP-v485-70e3db4

Current Mac-side evidence
-------------------------
- v519 non-Windows release gate:
  logs\win-v519-stage6-v519-non-windows-release-gates-20260520.json
- v519 post-docs-only release gate:
  logs\win-v519-stage6-v519-post-docs-only-gates-20260520.json
- v519 package report:
  docs\reports\2026-05-20-v519-vocational-practice-basic-info-filter-package.md
- final objective checklist:
  docs\reports\eidp-current-objective-evidence-checklist.md

Current Windows blocker
-----------------------
Windows remote execution is not currently available from the Mac side.

Latest read-only Mac probe after v518 PR refresh:
- Mac IPv4: 192.168.10.68
- ARP candidates:
  192.168.10.12, 192.168.10.70, 192.168.10.71, 192.168.10.72,
  192.168.10.73
- TCP/22: no candidate accepted SSH
- TCP/135, 139, 445, 3389, 5985, 5986: no candidate exposed a usable remote
  execution or file-transfer service in the probe

Before v519 Windows smoke can continue, on the Windows operator PC either:

1. provide the current Windows IPv4 address on the same LAN as the Mac; or
2. run the OpenSSH checks below in an elevated PowerShell window and report the
   output.

PowerShell checks to run on Windows
-----------------------------------
Run PowerShell as Administrator:

```powershell
Get-NetIPAddress -AddressFamily IPv4 |
  Select-Object InterfaceAlias,IPAddress,PrefixLength

Get-Service sshd

Get-NetFirewallRule -DisplayName "*OpenSSH*" |
  Select-Object DisplayName,Enabled,Direction,Action,Profile
```

If OpenSSH Server is missing or stopped, restore it:

```powershell
Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
Start-Service sshd
Set-Service -Name sshd -StartupType Automatic

New-NetFirewallRule `
  -Name OpenSSH-Server-In-TCP `
  -DisplayName "OpenSSH Server (sshd)" `
  -Enabled True `
  -Direction Inbound `
  -Protocol TCP `
  -Action Allow `
  -LocalPort 22
```

Then send back:
- Windows IPv4 address
- `Get-Service sshd` output
- firewall-rule output

Required v519 side-by-side smoke after remote access returns
-----------------------------------------------------------
Keep the active v485 scheduled task preserved unless an explicit promotion
decision is made.

Minimum evidence to collect:
- `validate_install.bat` output JSON
- OCR runtime validator output, if OCR remains in v1.0 scope
- UI health/root smoke
- Excel smoke
- bounded FY2026/R8 weekly canary
- active-task recovery proof showing the active task still points to v485
- Stage 6 evidence ZIP
- Stage 6 evidence verifier JSON

Release decision required before owner sign-off
-----------------------------------------------
If the release decision is strict FY2026/R8:
  - do not request owner sign-off yet;
  - wait until public FY2026/R8 target PDFs are available enough for strict
    yield to reach 60%.

If the release decision is publication-lag exception:
  - fill and approve:
    docs\reports\2026-05-19-publication-lag-release-exception-record.md
  - acknowledge FY2026/R8 strict yield is below gate;
  - use mature-year proof:
    logs\mature-year-acquisition-proof-fy2025-release-exception-v497-20260519.json
  - then schedule the owner real cycle.

Required files to return after owner real cycle
----------------------------------------------
- data\output\last_run.json
- latest logs\run-*.log
- latest logs\diagnostics-*.txt
- latest logs\stage6-evidence-*.zip
- latest logs\stage6-evidence-verify-*.json
- completed eidp-operator-e2e-template.md with KPI rows and sign-off
- ManualActionLog / JSONL audit proof: audit page status,
  manual_action_log count, outbox count before/after flush, and action_id
  consistency
- Excel proof: output workbook path or redacted workbook metadata, plus
  consistency evidence that Excel-ready coverage in the owner template matches
  the current DB/run metrics

Do not approve v1.0 from these alone
------------------------------------
- Mac clean ZIP build
- GitHub CI success
- Windows setup validator ok=true
- Read-only browser/UI health smoke
- Bounded weekly canary
- Mature-year proof JSON by itself
- Diagnostic evidence ZIP without a real owner cycle
- A NOT_APPROVED publication_lag exception record

Release approval still requires either:

- strict FY2026/R8 gate passing at production scale; or
- explicit publication_lag exception approval plus measured owner-cycle evidence
  verified by scripts\verify_stage6_return.py.
