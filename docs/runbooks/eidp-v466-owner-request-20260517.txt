EIDP v466 Owner / Operator Real-Cycle Request
Date: 2026-05-17

Purpose
-------
v466 is ready for Windows side-by-side preflight, then active-lane promotion
only after explicit approval. This is not another Mac-only proof and it is not
v1.0 approval.

Use this Windows package
------------------------
Package:
  C:\EIDP-staging\eidp-windows-v466.zip

Expected SHA256:
  8712c5b2687fa34de35c35a52b7df8bf8fe8f2ad82f153c30d24d551ac503db5

Side-by-side root:
  %USERPROFILE%\EIDP-v466-9a5d50b

Fallback root:
  %USERPROFILE%\EIDP-v460-01e4427

Short checklist
---------------
1. Follow:
   C:\EIDP-staging\v466-operator-docs-20260517\eidp-v466-active-promotion.md

2. Run the side-by-side setup preflight first. Confirm:
   - SHA256 matches
   - validate_install.bat returns ok=true
   - stage6_recovery_check.bat restores/confirms v460 fallback after preflight

3. Promote v466 to active only after explicit approval.

4. After v466 is active, open:
   %USERPROFILE%\EIDP-v466-9a5d50b\EIDP-start.bat

5. Confirm the browser opens:
   http://127.0.0.1:8501

6. Confirm the UI shows:
   target fiscal year 2026 / Reiwa 8

7. Complete the real workflow in the browser UI:
   - school task board
   - PDF review and manual entry
   - fiscal-year judgment and correction
   - Excel preview and download
   - audit log and outbox flush
   - local bug-report page only if a failure occurs

8. Run:
   - EIDP-stage6-evidence.bat
   - EIDP-stage6-verify-evidence.bat

9. Fill:
   C:\EIDP-staging\v466-operator-docs-20260517\eidp-operator-e2e-template.md

Required files to return
------------------------
- Latest logs\diagnostics-*.txt
- Latest logs\stage6-evidence-*.zip
- Latest logs\stage6-evidence-verify-*.json
- data\output\last_run.json
- Latest logs\run-*.log if present
- Completed KPI/sign-off rows from eidp-operator-e2e-template.md

Required KPI / sign-off fields
------------------------------
- target_pdf_auto_yield_pct
- operator_reviewable_yield_pct
- ship_gate_status
- ship_readiness_rc
- pending JSONL outbox count after flush
- owner sign-off
- operator sign-off

Publication-lag note
--------------------
FY2026/R8 may still be publication-lag sensitive. A publication-lag exception
can be considered only when measured KPI values are present and a mature-year
acquisition proof JSON is supplied. The exception does not allow null KPI
values, ship_gate_status=not_measured, missing audit/outbox evidence, or
missing sign-off.

Do not approve v1.0 from these alone
------------------------------------
- Mac clean ZIP build
- GitHub CI success
- Windows setup validator ok=true
- Read-only browser navigation smoke
- Diagnostic evidence ZIP without a real owner cycle

These are support evidence only. The release decision needs the v466
real-cycle record and verifier-accepted evidence bundle.
