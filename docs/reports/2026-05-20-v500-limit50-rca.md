# v500 FY2026 Limit-50 Re-Probe RCA

Date: 2026-05-20
Package candidate: `dist/eidp-windows-v500.zip`
Windows root: `%USERPROFILE%\EIDP-v500-e79ac12-env0`
Run ID: `20260519_165814`

## Verdict

`BELOW_GATE`.

The fresh v500 side-by-side re-probe does not change the release decision. The
50-school sample completed successfully, but current FY2026/R8 strict and
Excel-ready yield remained `4.0%`, far below the `60.0%` ship gate.

## Evidence

| Artifact | Result |
| --- | --- |
| `logs/win-v500-stage6-v500-last-run-after-weekly-canary-limit50-20260520.json` | `status=success`, denominator `50`, strict/Excel-ready yield `4.0%`, operator-reviewable yield `56.0%`, `ship_gate_status=below_gate` |
| `logs/win-v500-stage6-v500-weekly-canary-limit50-run-20260520.log` | batch wrapper ended rc `0`; discovery crawled `56`, found `50`, downloaded `0` |
| `logs/win-v500-stage6-v500-recovery-probe-lock-after-limit50-canary-20260520.json` | `ok=true`, lock not held, active task still points to v485 |
| `logs/win-v500-stage6-v500-weekly-canary-limit50-rca-batch-plan-20260520.json` | RCA batch plan with 20 items / 50 total candidates |

## RCA Summary

RCA batch buckets:

| Bucket | Count |
| --- | ---: |
| `non_target_candidates_only` | 17 |
| `target_form_without_year_evidence` | 3 |

RCA evidence-row PDF types:

| PDF type | Count |
| --- | ---: |
| `non_target` | 194 |
| `target` | 6 |

Top RCA evidence-row reasons:

| Reason | Count |
| --- | ---: |
| `pre_filtered_non_target_hint` | 179 |
| `candidate_school_mismatch` | 15 |
| `target_fiscal_year_not_detected` | 6 |

The three target-like items were all `target_form_without_year_evidence`; they
were not accepted as strict FY2026/R8 success. The dominant failure mode in this
fresh sample is still non-target material or target-like material without safe
current-year evidence, not a Windows packaging or launcher failure.

## Release Impact

This re-probe reinforces the existing no-go state:

- v500 is a valid candidate package for side-by-side validation.
- v500 still does not satisfy the strict FY2026/R8 release line.
- v1.0 remains blocked unless FY2026/R8 strict yield reaches `60%` at
  production scale or the owner explicitly approves the `publication_lag`
  release exception and completes the owner real cycle.
